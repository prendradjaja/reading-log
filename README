To actually use:

    Paste spreadsheet into data.tsv (tab-separated!)
    make run

For convenience during development:

    nnoremap ZW :w<CR>:!make run<CR>
    nnoremap ZM :w<CR>:!make run-diff<CR>
